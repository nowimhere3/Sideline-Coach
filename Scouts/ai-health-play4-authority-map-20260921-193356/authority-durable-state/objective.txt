AI Health Play 4 reconnaissance only. Architecture is already decided. Map the current Sideline-owned durable-state and process lifecycle seams relevant to persisting the future global AI Health Authority under the existing ~/.sideline ownership model. Identify exact helpers/conventions for Sideline directories, JSON state files, startup restore, safe writes, daemon stop/shutdown flushing, diagnostics, and directly relevant tests. Also identify any existing code that must NOT be reused because it is Game-scoped rather than Sideline-global. Do not invent persistence semantics, implement anything, inspect provider watchers, cross-device sync, AUTO/CONSERVE, or Plays 5-6. Report FACT / CONTRADICTION / UNKNOWN and exact coordinates.
