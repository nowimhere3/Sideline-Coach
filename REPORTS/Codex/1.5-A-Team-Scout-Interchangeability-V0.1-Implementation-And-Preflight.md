HERE'S THE REPORT

REPORT TIMESTAMP:
2026-09-16 07:43 MDT

REPORT NAME:
A-Team Scout Interchangeability V0.1 Implementation And Preflight

REPORT NUMBER:
1.5

REPORT FILE:
1.5-A-Team-Scout-Interchangeability-V0.1-Implementation-And-Preflight.md

REPORT TYPE:
Implementation / Verification

AGENT:
Codex

MODEL:
GPT-5.6 Sol

ROLE:
Integration / Player-Abstraction / Field-Proof Engineer

LOCAL SLICE / STAGE:
A-Team Scout Interchangeability Proof V0.1

# Outcome

PARTIAL. The provider-neutral abstraction, two executor boundaries, common telemetry/result contract, read-only adapter policies, deterministic Play identity, artifact isolation, secret redaction, and backward compatibility are implemented and automated-test proven.

The live two-Player field proof was not launched from Codex. Neither `GEMINI_API_KEY` nor `GOOGLE_GENERATIVE_AI_API_KEY` was available to this process. Per the credential handoff contract, no credential was requested in chat, no provider substitution occurred, and the official Play ID remains unused for one clean human-launched field run.

# Architecture Seam

`src/scout-play.ts` now owns the semantic Play independently of every Player:

- one normalized, deeply frozen ScoutPlay;
- explicit objective, scope, non-goals, read-only authority, evidence contract, and report contract;
- deterministic canonical JSON and SHA-256 Play identity;
- one semantic prompt rendered once and hashed once.

Provider and harness facts live below that seam in `src/scout-interchangeability-runner.ts`:

- Gemini Flash Direct: OpenCode -> Google Gemini API -> `google/gemini-3.8-flash`;
- AntiGravity: existing Structured Print control -> AntiGravity-selected `gemini-3.8-flash` at Medium effort.

The manifest contains no Player, provider, model, CLI syntax, transport, account, project, or credential field. Both executors receive the same frozen envelope object and exact semantic prompt bytes.

# Field-Proof Play

Play ID: `TTA-A-Team-Scout-Interchangeability-V0.1__2026-09-16_0714_MDT`

Game: `C:\Users\dmcal\Documents\GitHub\Trend and Tap Assist`

Task class: bounded code reconnaissance.

Objective: determine whether current repository evidence treats the exact literal `You broke the ice!` as a Full Transcript Sync completion sentinel, transcript content, or both, while tracing only immediate files, symbols, tests, and FILE examples.

The Play is defined once in `tools/scouts/a-team-interchangeability-play.json`. The official Game workspace and durable Scout evidence folder did not exist at preflight and were deliberately not created early.

# Player Routes

## Gemini Flash Direct

- Harness: OpenCode 1.18.31.
- Provider: Google Gemini API directly; OpenRouter is not in this route.
- Exact required model: `google/gemini-3.8-flash`.
- Authority: deny-by-default custom primary Scout agent; only read, glob, grep, and list are allowed; external-directory access and mutating tools are denied.
- Compatibility gate: the runner must observe the exact model in `opencode models google` before inference. It will not substitute a neighboring Gemini model.
- Current boundary: PROVIDER BLOCKED in the Codex process because no supported process-local Google credential was visible. No request was made.

## AntiGravity

- Harness/provider: AntiGravity CLI 1.2.4 through the existing Structured Print Player Control implementation.
- Requested model family/effort: `gemini-3.8-flash` / `medium`.
- Live capability result: `gemini-3.8-flash-medium` was present in `agy.exe models` at 2026-09-16 07:43 MDT.
- Authority: the new Scout authority maps to AntiGravity's native `--mode plan --sandbox --disable-slash-commands`; the existing full-autonomy and accept-edits routes are unchanged.
- Current boundary: not launched. The coordinated field run was held so the same official Play folder can capture both Players once, in the required order, after secure Google credential handoff.

# Common Result And Game Film

Every attempt normalizes to one factual telemetry shape containing:

- ScoutPlay ID, canonical Play hash, and semantic prompt hash;
- Player, harness, provider, model, and effort;
- task class, queued/start/end time, and duration;
- lifecycle state, process exit code, and signal;
- provider/rate-limit evidence;
- report-created and read-only-contract evidence;
- failure boundary and independent output/report paths.

Human/Codex evaluation is explicitly separate from measured telemetry: structural usability, missing evidence, unsupported-claim observation, deeper-review need, and a mechanically matched Game artifact. No numerical performance score is invented.

The runner writes canonical Play identity, one prompt, per-Player raw output/telemetry, completion JSON, independent successful reports, and `INTERCHANGEABILITY-COMPARISON.md` beneath the Play evidence folders. It never overwrites an existing Play folder.

# Read-Only Boundary

Gemini Direct is guarded by static deny-by-default OpenCode agent validation before credential/model checks. AntiGravity uses the installed CLI's native Plan, Sandbox, and slash-command-disable flags through the existing controlled process seam. Provider-specific policies remain below the shared Play and are not weakened to look identical.

This is harness permission enforcement, not a claim that Windows has placed both provider processes inside an independently verified OS filesystem jail. The live field run must still inspect Game repository state and distinguish Scout artifacts from unrelated concurrent development.

# Files Added

- `src/scout-play.ts`
- `src/scout-interchangeability-runner.ts`
- `tools/scouts/a-team-interchangeability-play.json`
- `tools/scouts/opencode-interchangeability/agents/sideline-gemini-direct-scout.md`
- `tools/scouts/run-interchangeability-proof.mjs`
- `tools/scouts/run-a-team-interchangeability.ps1`
- `test/scout-play-interchangeability.test.mjs`
- this report

# Files Modified

- `src/player-control/contract.ts`: added a dedicated read-only Scout authority.
- `src/player-control/structured-print.ts`: mapped that authority for AntiGravity and exposed bounded process lifecycle telemetry.
- `package.json`: added `scout:interchangeability`.
- `Docs ANCHOR/ARCHITECTURE-BREADCRUMBS.md`: added the distributed WAS / IS / WHY invariant beside the canonical architecture.

The existing OpenCode/OpenRouter Scout Runner and its manifests were not migrated or replaced.

# Automated Proof

Focused abstraction and legacy Runner command:

`node --test test\scout-play-interchangeability.test.mjs test\scout-play-runner.test.mjs`

Result: 12 passed, 0 failed.

The new six tests prove deterministic normalization, one immutable identity, byte-identical semantic prompts for two executors, mutation rejection, common result normalization, independent failure handling, continued execution, secret redaction across all generated artifacts, shared structural evaluation, direct-agent read-only preflight, and AntiGravity read-only authority mapping. The existing six Scout Runner tests remained green.

Full relevant regression:

`npm run compile; npm test`

Result: compile passed; 928 tests passed, 0 failed, 0 skipped, 0 cancelled.

Additional preflight:

- PowerShell secure launcher parse errors: 0.
- Manifest JSON validation: passed.
- OpenCode version: 1.18.31.
- AntiGravity version: 1.2.4.
- AntiGravity exact requested family/effort visible: yes.
- `GEMINI_API_KEY` visible to Codex: no.
- `GOOGLE_GENERATIVE_AI_API_KEY` visible to Codex: no.
- official workspace already used: no.
- official durable evidence folder already used: no.
- provider inference requests made by Codex: 0.

# Secure Field Launch

From the already intended PowerShell terminal at `PS C:\Users\dmcal\Documents\GitHub\SidelineCoach>`, run exactly:

```powershell
& ".\tools\scouts\run-a-team-interchangeability.ps1"
```

The script prompts locally for the Google key as a SecureString, exposes it only to that PowerShell process and its child, uses an external OpenCode database, runs Gemini Flash Direct first and AntiGravity second with `maxConcurrency = 1`, then removes both credential environment variables and zeroes the unmanaged key buffer. It does not print or persist the key.

The runner will create and retain:

- `C:\Users\dmcal\Documents\GitHub\Trend and Tap Assist\Scouts\TTA-A-Team-Scout-Interchangeability-V0.1__2026-09-16_0714_MDT\`
- `C:\Users\dmcal\Documents\GitHub\SidelineCoach\REPORTS\Scout Only\TTA-A-Team-Scout-Interchangeability-V0.1__2026-09-16_0714_MDT\`

# Anything Not Proven

- Neither Player has yet executed the semantic Play in the official field run.
- Direct Google authentication and exact live `google/gemini-3.8-flash` availability remain unproven until the keyed command runs.
- Real-source reading, report usefulness, common result compliance, process retirement, provider errors, quota behavior, and Game non-mutation remain field questions.
- No `INTERCHANGEABILITY-COMPARISON.md` or first game-film telemetry exists yet because creating partial official evidence before secure launch would consume the no-overwrite Play ID.

# WAS / IS / WILL BE

## WAS

Scout assignments coupled semantic intent to an OpenCode agent/provider/model invocation.

## IS

Sideline owns one immutable, hashed semantic ScoutPlay. Direct Gemini and AntiGravity are separate executor adapters beneath it. The abstraction and compatibility behavior are automated-test proven; live two-Player interchangeability is credential-blocked and not claimed.

## WILL BE

Run the one secure command, inspect the raw reports and comparison evidence, confirm process retirement and Game boundaries, then record the live verdict. Do not build automatic selection, rotation, scoring, or routing in this Play.

# Final Verdict

A-TEAM SCOUT INTERCHANGEABILITY V0.1

PROVIDER-NEUTRAL SCOUTPLAY: PASS

SAME PLAY IDENTITY USED BY BOTH: NO — mechanically guaranteed, but not yet live-executed

GEMFLASH ROUTE: BLOCKED

ANTIGRAVITY ROUTE: BLOCKED — coordinated launch held; live capability is available

READ-ONLY SCOUT CONTRACT: PARTIAL — mechanically configured; live Game boundary not yet verified

COMMON RESULT CONTRACT: PASS

EXISTING SCOUT RUNNER PRESERVED: YES

FIRST GAME FILM RECORDED: NO

INTERCHANGEABILITY: PARTIAL

NEXT RECOMMENDED PLAY: Run the single secure Formation command above and inspect the resulting two-Player comparison evidence; do not add routing or rotation.

No commit, push, stash, reset, or provider substitution was performed.

REPORT NAME: A-Team Scout Interchangeability V0.1 Implementation And Preflight
REPORT TIMESTAMP: 2026-09-16 07:43 MDT
