REPORT TIMESTAMP:
2026-09-16 12:47:19 MDT

REPORT NAME:
Plumbing SLC Bootstrap Implementation

STAGE / SLICE:
S11.1

REPORT FILE:
S11.1-Plumbing-SLC-Bootstrap-Implementation.md

REPORT TYPE:
Implementation

AGENT:
Codex

MODEL:
GPT-5.6 Sol

REASONING / EFFORT:
High

ROLE:
Bounded Filesystem Implementation Worker

# VERDICT

PASS. S11.1 implements the approved S11.0 amendment on the expected `q2.8-multigame-field-debug` branch. A fresh, filesystem-unconfigured Game now bootstraps `Plumbing SLC/Reports`, `Plumbing SLC/SOP`, and only roster-required provider lanes. Mature Games are adopted before creation and are not migrated. Schema v1 and the existing exact-Game Stadium mutation authority remain intact.

# WHAT WAS

S8 could authorize and create `Reports-SLC/`, then ensure stable provider lanes beneath the canonical Reports coordinate. Reports and SOP decisions already belonged to the durable per-Game `GameFilesystemContract`; S7, S9, and S10 already consumed arbitrary nested Game-relative coordinates.

# WHAT CHANGED

- Reserved Plumbing parent/child evidence is inspected read-only, including empty and partial structures, collisions, casing variants, and escaping links.
- Fresh exact-Game evidence may produce an ephemeral Plumbing bootstrap plan. The plan is not a new durable coordinate and is never reconstructed from stale intent alone.
- The existing S8 Stadium ensure seam now preflights every required parent, child, and missing lane path before ordered creation.
- Successful SOP creation is recorded with existing `created` provenance and `ready` state.
- Partial safe Plumbing structures are completed; ambiguity or unsafe evidence authorizes no mutation.
- Legacy `create-reports-slc` state remains loadable and is reconciled against fresh evidence before action.

# FILES CHANGED

- `src/game-filesystem-contract.ts` — S11 default-path constants, pure evidence decisions, and ephemeral bootstrap authorization; schema remains v1.
- `src/game-files.ts` — reserved Plumbing evidence and safety inspection; existing bounded path checks and ensure primitive are reused.
- `src/control-plane/game-filesystem-coordinator.ts` — returns the ephemeral plan and records created/attention SOP truth.
- `src/control-plane/daemon.ts` — exact-Game preflight, ordered ensure, partial-failure recording, and convergence through the existing contract apply path.
- `test/s8-reports-slc-bootstrap.test.mjs` — focused S8/S11.1 mutation and convergence proof.
- `Docs ANCHOR/ARCHITECTURE-BREADCRUMBS.md` — S11.0 proposal promoted to proven `IS (S11.1)` only after green proof.
- This report.

# NEW-GAME BOOTSTRAP

For a Game with neither durable authoritative coordinate nor meaningful competing Reports/SOP evidence, the implemented result is:

```text
Plumbing SLC/
  Reports/
    <rostered provider lanes only>/
  SOP/
```

No `Diagnostics/`, placeholder file, metadata file, `.gitkeep`, SOP content, model lane, effort lane, instance lane, or terminal lane is created.

# EXISTING-GAME COMPATIBILITY

Detection and adoption run before creation. Existing `Reports/`, `REPORTS/`, `Reports-SLC/`, `Docs REPORT/`, nested canonical Reports roots, `Project SOP/`, `Onboarding-Docs/`, `Onboarding-SOP/`, and `SOP/` retain their exact paths and casing. An existing Reports root without SOP does not cause Plumbing to be created merely to supply SOP. Reports and SOP remain independent coordinates.

# PARTIAL PLUMBING STRUCTURES

- Empty `Plumbing SLC/` gains the two approved children and roster lanes.
- Existing `Plumbing SLC/Reports/` is adopted; missing SOP and roster lanes are ensured.
- Existing `Plumbing SLC/SOP/` is adopted; missing Reports and roster lanes are ensured.
- `Plumbing SLC/Reports/` may coexist canonically with an existing external SOP, and vice versa.
- Competing meaningful Reports roots produce Needs Choice and zero bootstrap mutation.
- Parent/child file collisions, multiple case variants, unsafe links, blocked paths, incomplete preflight, offline/wrong-Game state, and unsupported Stadium capability authorize no bootstrap mutation.

# REPORTS-SLC LEGACY COMPATIBILITY

`Reports-SLC/` remains a fully recognized valid layout. Existing Games keep it as their canonical root, receive provider lanes through the established mechanism, and are never renamed, copied, aliased, warned into migration, or converted to Plumbing. Legacy schema-v1 contracts continue to load.

# SOP CREATION

The clean or approved partial bootstrap creates an empty `Plumbing SLC/SOP/` directory only. Verified success records `sop.path = Plumbing SLC/SOP`, provenance `created`, and state `ready` through existing `FolderProvenance` semantics. No SOP content is generated.

# PROVIDER LANES

Lanes are keyed by existing stable provider identity and created only for roster entries with active instances. Duplicate instances share one lane; roster shrink never deletes historical lanes. No AntiGravity lane is created when the roster does not require it. Repeated ensure is idempotent.

# PREFLIGHT / SAFETY

Before clean/partial bootstrap mutation, Control Plane asks the exact authoritative Stadium to check the Plumbing parent, Reports child, SOP child, and every missing provider-lane target. Every requested check must be present and either an existing folder or safely missing. Collision, blocked/escaping path, unknown response, incomplete response, unsupported feature, foreign Game response, or disconnected Stadium fails closed. The existing containment-aware ensure primitive then verifies every created directory. Nothing is overwritten, deleted, renamed, or moved.

# PARTIAL FAILURE BEHAVIOR

An unexpected I/O failure stops later planned mutation, preserves any directories already created safely, records truthful Needs Attention state, and performs no destructive rollback. A later fresh reconcile may complete the partial structure idempotently.

# CONTRACT / REVISION CONVERGENCE

`GAME_FILESYSTEM_SCHEMA_VERSION` remains `1`. Existing `reports.path` and `sop.path` fields express both nested coordinates; no `plumbingRoot` or second durable owner was added. The bootstrap plan exists only for the fresh reconcile result. Created Reports/SOP and lane results are persisted through the existing coordinator. Repeated ensure with no missing work creates nothing and produces no unnecessary contract revision churn.

# S7 CONVERGENCE

No S7 redesign or default-path special case was added. Once the contract records `reports.path = Plumbing SLC/Reports`, the existing apply/cache/watcher path consumes that coordinate. The focused wire proof confirms canonical lane attribution beneath it.

# S9 CONVERGENCE

No S9 redesign or path joining special case was added. The existing destination resolver consumes the same ready contract and lane records, producing `Plumbing SLC/Reports/Codex/`, `.../Claude/`, or another rostered stable provider lane.

# S10 CONVERGENCE

No S10 UI redesign or Plumbing-specific control was added. Existing Game Setup projection shows both nested paths with `Created by Sideline`; Browse / Change, independent Reports/SOP human overrides, and Restore detected folder continue to use the existing authority seams. Human choice wins and old Plumbing directories are not reclaimed or deleted.

# EXACT GAME ISOLATION

Inspection, preflight, ensure, recording, and contract apply all carry the explicit Game id and use the exact authoritative Stadium session. Focused proof bootstraps Game A while Game B remains byte-for-byte unchanged. Offline, unknown, mismatched, or unsupported Game/Stadium states never fabricate success.

# IDEMPOTENCY

Repeated reconcile/ensure does not duplicate folders or lanes, does not delete lanes after roster shrink, and does not burn a revision when no durable truth changes. Safe partial state is the input to the next bounded reconcile rather than a reason to restart or normalize the tree.

# NO-MIGRATION PROOF

Fixtures with mature Reports/SOP trees preserve their original snapshots and historical file bytes. Tests cover `Reports`, `REPORTS`, `Reports-SLC`, `Docs REPORT`, all recognized SOP families, independent mixed coordinates, competing roots, overrides, and Restore. No production path moves, copies, merges, renames, cleans, or deletes established content.

# TESTS

- `npm.cmd run check` — PASS.
- `npm.cmd run compile` — PASS.
- `node --test test/s8-reports-slc-bootstrap.test.mjs` — 51 PASS, 0 FAIL.
- Focused S6/S7/S8/S9/S10/S11 command over seven filesystem/report suites — 171 PASS, 0 FAIL.

The focused matrix covers clean bootstrap, all recognized legacy roots, all approved partial Plumbing cases, collisions, link escape, casing, exact-Game isolation, provider identity/lifecycle, human overrides, Restore, legacy pending intent, partial I/O failure, complete preflight, S7 watcher/cache convergence, S9 destination convergence, and S10 projection.

# FULL TEST RESULT

`npm.cmd test` — 951 PASS, 0 FAIL, 0 skipped, 0 cancelled.

# GIT DIFF CHECK

`git diff --check` — PASS. Git emitted only existing working-copy LF-to-CRLF notices; it reported no whitespace errors. No unrelated dirty work was discarded or rewritten.

# BREADCRUMB IMPACT

The adjacent architecture breadcrumb now marks the bounded behavior as `IS (S11.1)`. It explicitly retains no-migration, schema-v1 ownership, permanent `Reports-SLC` compatibility, human authority, and future-only/uncreated Diagnostics.

# HUMAN FIELD PROOF NEEDED

Yes. Automated wire and filesystem tests are complete, but a human should still exercise one disposable, genuinely unconfigured Game in a live Stadium: connect a small provider roster, verify the exact created tree, confirm Game Setup labels, confirm Incoming watches the nested Reports root, dispatch one controlled report to its provider lane, then verify Browse / Change and Restore behavior. This Play did not create Plumbing in any real Game repository.

# BOUNDARIES PRESERVED

Control Plane remains the sole durable decision owner; Stadium remains the exact-Game filesystem mutation owner. S7, S9, and S10 retain contract-indirected behavior. No Diagnostics, SOP content, Game identity change, self-hosting fix, report-search UI, automatic numbering, general-purpose mutation API, commit, push, reset, stash, or clean was performed.

# NEXT APPROVED PLAY

Recommended next bounded Play: `S11.2 — Plumbing SLC Human Field Proof`. It should validate the live Dad-facing and Player/Incoming round trip in a disposable new Game without expanding S11.1 architecture or authorizing migration. It was not started here.

REPORT NAME: Plumbing SLC Bootstrap Implementation
REPORT TIMESTAMP: 2026-09-16 12:47:19 MDT
