REPORT TIMESTAMP:
2026-09-19 22:55 MDT

REPORT NAME:
Live Player Terminal — UI Polish V0.1b

REPORT NUMBER:
S42.0

REPORT FILE:
S42.0-Live-Player-Terminal-UI-Polish-V0.1b.md

REPORT TYPE:
Implementation

AGENT:
Claude

MODEL:
Sonnet 5

ROLE:
Surgical UI polish / terminology correction

LOCAL SLICE / STAGE:
Continues S41.0 (Live Player Console — First-Down Slice V0.1)

RM-1:
SKIPPED. Terminology/CSS pass on a field-tested slice; no reconnaissance. No transport, redaction, stdout capture or copy behavior touched.

==================================================
RESULT
==================================================

**PASS.** Focused tests 7/7; full suite 1210/1210. Nothing committed or pushed.

==================================================
FILES CHANGED
==================================================

- `src/public/index.html` — Settings copy, button labels, aria labels, two small CSS rules, one class on the toggle row.
- `test/live-player-console-first-down.test.mjs` — labels updated; assertions added for Settings copy, aria labels, toggle row class; new LPC-4b for compact/right-aligned CSS.

==================================================
TERMINOLOGY CHANGES
==================================================

| Where | Was | Now |
|---|---|---|
| Settings title | Live Player Console | **View Player Terminal** |
| Settings copy | Show an expandable live developer view inside active Player cards. | **Show a live Player terminal view inside active Player cards.** |
| Settings checkbox aria-label | Live Player Console | View Player Terminal |
| Player card button | Expand Console / Collapse Console | **Expand / Collapse** |
| Button aria-label | …the live console for {name} | …the live terminal for {name} |
| Region aria-label | Live console for {name} | Live terminal for {name} |
| Copy button aria-labels | … console content for {name} | … terminal content for {name} |

Deliberately NOT renamed (no user-facing effect, would be pure churn): `livePlayerConsole` preference key, element ids, `.play-console*` classes, `consoleExpandedByInstance`, `consoleEntriesFor`, focus keys, and the S41.0 breadcrumb block. The Settings HTML comment notes the internal name. Placeholder text "Waiting for live execution activity…" unchanged.

==================================================
ALIGNMENT / SIZING CHANGES
==================================================

- **Expand / Collapse:** now sits in its own row (`play-strip-actions play-terminal-toggle-row`), `justify-content: flex-end`.
  Button: `min-height 32px` (was 40), `padding 4px 10px` (was 8px 12px), `font-size .78rem` (was .84rem), `flex: 0 0 auto` so the existing ≤460px `flex: 1 1 auto` rule no longer stretches it across the card. Row top margin trimmed to 2px.
- **Copy All / Copy New:** `.play-console-actions` now `justify-content: flex-end`; still the last element of the expanded region, still disabled.
- **Terminal body:** untouched — 14.5px/1.6 (16.5px/1.65 under 620px), scrollable, honest placeholder.

==================================================
FOCUSED VERIFICATION
==================================================

`node --test test/live-player-console-first-down.test.mjs` — 7/7:
1. Dev Mode OFF: no setting, no control (unchanged).
2. Dev Mode ON, feature OFF: setting visible, unchecked, no control; Settings HTML reads `View Player Terminal` + the new supporting copy, and the old title is gone.
3. Both ON: control reads `Expand`; expanded reads `Collapse`; toggle row has the compact right-aligned class; region label says "Live terminal for Claude 1"; exact `instanceId`, `◉ Working` row and elapsed origin unchanged; no server requests on toggle.
4. Copy All / Copy New disabled, last in the region; expansion survives roster rebuild per exact instance.
4b. (new) CSS assertions: toggle row right-aligned; button 32px / 4px 10px / not stretched; copy row right-aligned.
5–6. Preference save/persistence unchanged.

Full suite (`npm test`): 1210 / 1210.

==================================================
NEEDS HUMAN VISUAL JUDGMENT
==================================================

The DOM stub proves structure, class names and the CSS rules as text — not how they render. Please eyeball on desktop and phone:
- Is 32px tall / .78rem the right "compact but still tappable" point? (One-line tweak in `.play-strip-actions.play-terminal-toggle-row button` if not.)
- Does the right-aligned Expand row read as secondary beneath the timer row?
- Are the right-aligned Copy buttons comfortable at the bottom of the region on a narrow screen?

==================================================
RECOMMENDED NEXT PLAY
==================================================

**Live Player Terminal V0.2 — sanitized exact-Player live activity transport + Copy All / Copy New.**
Redaction/allow-list boundary first, then `instanceId`-scoped sanitized lines through the existing Player `ControlEvent` pipeline into `consoleEntriesFor()`, then enable the copy buttons (Copy New cursor advances only after a successful clipboard write).

REPORT NAME: Live Player Terminal — UI Polish V0.1b
REPORT TIMESTAMP: 2026-09-19 22:55 MDT
