REPORT FILE:
Q2.4-Routing-Intelligence-And-Capability-Implementation.md

REPORT TIMESTAMP:
2026-09-11 16:51:00 -06:00

---

# SIDELINE COACH — Q2.4: ROUTING INTELLIGENCE + LIVE CAPABILITY IMPLEMENTATION

**Agent / model:** AntiGravity / Gemini 3.8 Flash (High)  
**Role:** Bounded Senior Implementation Worker  
**Project:** Sideline Coach  
**Dev Stadium Repository:** C:\Users\dmcal\Documents\GitHub\SidelineCoach, branch main  
**Baseline Commit:** 8e79e9882a174bf9052b6623e1cfcf0d79fa5527 (*Scout Q2.3 Routing Intelligence & Capability Discovery Architecture*)  
**Verdict:** PASS — AUTOMATED PROOF COMPLETE / HUMAN FIELD PROOF PENDING  

---

## 1. VERDICT & EXECUTIVE SUMMARY

### VERDICT: PASS — Q2.4 IMPLEMENTATION COMPLETE

Quarter 2 Play Q2.4 (**ROUTING INTELLIGENCE + LIVE CAPABILITY IMPLEMENTATION**) is fully implemented according to the approved blueprint in REPORTS/AntiGravity/Q2.3-Routing-Intelligence-And-Capability-Architecture.md and all four explicit architectural amendments.

All 119 tests across the Sideline Coach test suite pass with 0 failures, including 33 newly authored unit and browser harness tests in 	est/routing-intelligence.test.mjs.

### Core Achievements

1. **Replaced Prototype Selectors with Capability-Backed Routing:**
   - Stale hardcoded model lists have been replaced with live discovered models and reasoning efforts.
   - Dual-mode routing (AUTO and MANUAL) is live in both server routing policies and browser UI.

2. **Critical Amendment 1 (Capability Failure Semantics) Enforced:**
   - **MANUAL Mode:** If capability discovery fails or is unavailable, MANUAL falls back to Provider Default with a clear warning badge (Live discovery unavailable · using Provider Default).
   - **AUTO Mode:** If live capability discovery is unavailable, AUTO stops safely, blocks dispatch with HTTP 400 Bad Request, and informs the human: Live routing capabilities are unavailable. Refresh capabilities or switch to Manual. No synthetic models are hallucinated and no stale guesses are used.

3. **Critical Amendment 2 (No Fossilized Model Names) Enforced:**
   - Tasks are classified generically (rchitecture, implementation, quick, default) using deterministic heuristics.
   - The generic router classifies the task; the provider-specific policy (CodexRoutingPolicy) maps that classification to models and effort levels dynamically discovered from the provider's live catalog.
   - Selections are checked against the discovered catalog via safety validation.

4. **Critical Amendment 3 (Observation ≠ Policy) Respected:**
   - CapabilityService strictly observes and caches provider snapshots (ProviderCapabilitySnapshot).
   - Policy implementations (CodexRoutingPolicy, computeAutoRoute) decide player, model, and effort selection based on observed state.

5. **Critical Amendment 4 (Zero Capacity Routing) Respected:**
   - Capacity metrics and usage heuristics are strictly excluded from Q2.4 routing algorithms to avoid unmetered or asymmetric heuristics.

6. **End-to-End Live JSON-RPC Discovery & Semantic Delivery:**
   - CodexAppServerControl.queryCapabilities() queries ccount/read and model/list over stdio JSON-RPC without consuming model turns.
   - PlayerControl.deliver() passes semantic { model, effort } options directly into the provider's 	urn/start frame.
   - Legacy named terminals remain backward-compatible and explicitly badged in MANUAL.

---

## 2. ARCHITECTURAL CONTRACTS & COMPONENTS DELIVERED

### A. Pure TypeScript Capability Contracts (src/capability-types.ts)
Zero dependencies on VS Code modules, enabling complete off-line unit testability:
- CapabilityFreshness: 'live' (< 60s), 'cached' (60s – 10m), 'stale' (> 10m), 'unavailable' (missing/failed).
- ModelDescriptor: Normalized model metadata (id, displayName, isDefault, supportedEfforts, defaultEffort).
- ProviderCapabilitySnapshot: Provider-level snapshot (provider, uthenticated, ccountEmail, planType, models, observedAt, reshness).
- PlayerRoutingCapability: Player instance routing state (instanceId, playerType, 	ransport, ieldLabel, state, capability, ctiveModel, ctiveEffort).
- TaskClassification: 'architecture' | 'implementation' | 'quick' | 'default'.
- RoutingMode: 'auto' | 'manual'.
- RoutingDecision: Detailed staged route decision (mode, gameId, playerInstanceId, playerLabel, model, modelDisplayName, effort, eason, ationale, classification).

### B. In-Memory Capability Service (src/capability-service.ts)
- Configurable freshness TTLs (LIVE_TTL_MS = 60,000, CACHED_TTL_MS = 600,000).
- Dynamically recalculates freshness on read.
- Supports cache invalidation by provider or globally (invalidate()).
- Returns standardized unavailable snapshot when uninitialized or upon provider error.

### C. Deterministic Routing Policy Engine (src/routing-policy.ts)
- classifyTask(prompt): Deterministic keyword, line-count, and length heuristics.
- CodexRoutingPolicy: Provider-specific policy selecting models:
  - rchitecture → highest-tier reasoning model with ultra/max effort.
  - implementation → coding workhorse model with medium/high effort.
  - quick → fast responsive model with low effort.
  - default → provider default model.
- Validation: Verifies that chosen models actually exist in the live snapshot; if not found, cleanly falls back to Provider Default.
- computeAutoRoute(): Filters candidates by readiness, controlled certification, and Game scope, enforcing the fail-safe stop rule when capabilities are unavailable.

### D. Provider Adapter & Control Host Integration (src/player-control/)
- contract.ts: Extended PlayerControl.deliver(play, clientRef, options?: DeliverOptions) with { model?: string, effort?: string }.
- codex-app-server.ts:
  - Added 'model/list' to REQUEST_ALLOWLIST.
  - Implemented queryCapabilities() calling ccount/read and model/list.
  - deliver() forwards model and effort in the 	urn/start RPC params.
  - Normalizes OpenAI models and supported reasoning efforts into ProviderCapabilitySnapshot.
- host.ts: deliver() forwards options to adapter; exposes queryCapabilities(instanceId).

### E. Player Roster Integration (src/player-roster.ts)
- Instantiates CapabilityService.
- Automatically triggers capability refresh when a controlled player is added or restored (queryPlayerCapabilities()).
- Exposes getRoutingCapabilities(activeGameId) synthesizing instance state, controlled bindings, and capability snapshots.
- Exposes efreshCapabilities(provider?) for on-demand human or API refresh.

### F. Server API Endpoints (src/server.ts)
- State: Tracks outingMode: RoutingMode and manualSelection.
- POST /api/route: Sets routing mode (uto vs manual).
- POST /api/route/preview: Classifies prompt and returns dynamic route preview without dispatching.
- POST /api/capabilities/refresh: Triggers active capability scan across controlled players.
- GET /api/status: Surfaces outing: { mode, activeDecision, autoError, capabilities, manualSelection }.
- POST /api/dispatch: Respects mode; in AUTO, computes route, enforces fail-safe 400 on unavailable capabilities, and passes chosen model and effort to deliverControlled().

### G. Browser UX Enhancements (src/public/index.html)
- **Mode Toggle Bar:** [ AUTO ] [ MANUAL ] segmented toggle group + ↻ Refresh button.
- **AUTO Panel:**
  - Staged route chip: Codex 1 · Controlled · GPT-5.6-Sol · Medium.
  - Plain-English rationale banner: e.g. Implementation task · Standard coding model.
  - Customize link: Shifts to MANUAL mode with current AUTO choices pre-selected.
  - Warning banner: Displays when live capabilities are unavailable, disabling the Dispatch button.
- **MANUAL Panel:**
  - Cascading dependent selectors: Target Player → Model → Reasoning Effort.
  - Selecting a Player dynamically populates valid models for that provider.
  - Selecting a Model dynamically populates valid reasoning efforts for that model.
  - Uncertified legacy terminals display a distinct legacy transport notice.
  - Missing live discovery displays a warning badge: Live discovery unavailable · using Provider Default.
- Real-time preview: Typing in the prompt payload debounces and previews the AUTO staged route dynamically.

---

## 3. AUTOMATED PROOF SUMMARY

Total automated test suite passes with **119/119 passed, 0 failures**:

`	ext
✔ 1. classifyTask: architecture keywords produce architecture classification (2.2316ms)
✔ 2. classifyTask: prompt exceeding 3000 chars classifies as architecture (0.2764ms)
✔ 3. classifyTask: implementation keywords produce implementation classification (0.5488ms)
✔ 4. classifyTask: multiline prompt classifies as implementation (0.1836ms)
✔ 5. classifyTask: quick keywords produce quick classification (0.3996ms)
✔ 6. classifyTask: prompt under 80 chars without stronger keywords classifies as quick (0.159ms)
✔ 7. classifyTask: empty prompt or whitespace falls back to default (0.2224ms)
✔ 8. classifyTask: standard prompt without matching keywords falls back to default (0.3286ms)
✔ 9. CapabilityService: freshness is live within 60s (0.531ms)
✔ 10. CapabilityService: freshness is cached between 60s and 600s (0.2924ms)
✔ 11. CapabilityService: freshness is stale after 600s (0.1603ms)
✔ 12. CapabilityService: freshness is unavailable when observedAt <= 0 or explicit (0.2505ms)
✔ 13. CapabilityService: record and get preserves snapshot and dynamically refreshes freshness (0.257ms)
✔ 14. CapabilityService: get on unknown provider returns unavailable snapshot (0.1727ms)
✔ 15. CapabilityService: invalidate removes specific provider or all providers (0.5133ms)
✔ 16. CodexRoutingPolicy: architecture task selects ultra/max reasoning model (0.6096ms)
✔ 17. CodexRoutingPolicy: implementation task selects workhorse coding model (0.3056ms)
✔ 18. CodexRoutingPolicy: quick task selects fast responsive model (2.6027ms)
✔ 19. CodexRoutingPolicy: default task selects default model (0.2996ms)
✔ 20. CodexRoutingPolicy: empty models snapshot falls back to Provider Default (0.1532ms)
✔ 21. CodexRoutingPolicy: safety validation validates against live discovered catalog (0.4512ms)
✔ 22. computeAutoRoute: selects candidate and model matching classified task (0.6013ms)
✔ 23. computeAutoRoute: fails safe when no controlled player is on field (0.2396ms)
✔ 24. computeAutoRoute: fails safe when all controlled players are busy (0.1743ms)
✔ 25. computeAutoRoute: Critical Amendment 1 fail-safe stops safely when capabilities unavailable (0.1367ms)
✔ 26. CodexAppServerControl.queryCapabilities: queries account/read and model/list over JSON-RPC (128.5499ms)
✔ 27. PlayerControlHost.deliver passes model and effort in turn/start RPC params (129.8405ms)
✔ 28. PlayerControlHost.deliver without options omits model and effort in RPC params (105.7229ms)
✔ 29. Browser UI renders AUTO mode as default with chip and rationale (37.232ms)
✔ 30. Switching between AUTO and MANUAL toggles UI controls and updates mode (10.9143ms)
✔ 31. MANUAL dropdown cascades: selecting player loads models, selecting model loads efforts (14.3077ms)
✔ 32. AUTO mode displays warning banner when capabilities are unavailable (15.7808ms)
✔ 33. Refresh capabilities button fires POST /api/capabilities/refresh (7.0221ms)

ℹ tests 119
ℹ suites 0
ℹ pass 119
ℹ fail 0
`

---

## 4. MINIMAL HUMAN FIELD PROOF PLAN FOR [Extension Development Host] GS3

To confirm Q2.4 in the live VS Code execution environment:

1. **Launch Extension:**
   - Press F5 in VS Code to launch [Extension Development Host] GS3.
   - Open the browser at http://127.0.0.1:49152.

2. **Verify AUTO Staged Route:**
   - Observe the Outgoing card.
   - Confirm [ AUTO ] is active by default.
   - Put a controlled Codex Player on the field (Put on Field).
   - Confirm the staged route chip renders with live discovered Codex capabilities (e.g. Codex 1 · Controlled · GPT-5.6-Sol · Medium).
   - Type an architecture-focused prompt (e.g., Design the blueprint for multi-provider orchestration).
   - Observe the staged route update to high-reasoning model / effort with explanation.

3. **Verify AUTO Dispatch:**
   - Click Dispatch Play.
   - Verify the Q2.1 button lifecycle runner (Sending… → Received → Working… → Completed).
   - Verify the prompt auto-clears upon Received.

4. **Verify MANUAL Cascading Dropdowns:**
   - Click [ MANUAL ].
   - Verify Target Player selector shows the controlled Player.
   - Verify Model dropdown populates with the live discovered models.
   - Select a model (e.g. GPT-6-Astra) and verify Reasoning Effort dropdown shows only supported efforts (low, medium, high, extra-high, max, ultra).
   - Click ↻ Refresh and verify toast notifications confirm capability refresh without page reload.
