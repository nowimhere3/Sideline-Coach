REPORT TIMESTAMP:
2026-09-18 18:26 MDT

REPORT NAME:
Qwen + Inkling LIMITED Scorecard Forensic V0.1

REPORT NUMBER:
S28.0

REPORT FILE:
S28.0-Qwen-Plus-Inkling-LIMITED-Scorecard-Forensic-V0.1.md

REPORT TYPE:
Forensic

AGENT:
AntiGravity

MODEL:
Gemini 3.8 Flash

ROLE:
Forensic Scout

LOCAL SLICE / STAGE:
Combine V0.3 Evaluation Forensic

==================================================
RESULT
==================================================

FORENSIC COMPLETE. Root causes identified with exact code line citations, report text diffs, and node regex verification.

SUMMARY OF FINDINGS:
1. **The Exact Failure Mechanism:** Neither Qwen nor Inkling was classified `LIMITED` because of missing facts, inaccurate analysis, infrastructure failure, or lack of repository evidence. In fact, both models held the read-only contract, verified real repository files (`package.json`, `out/extension.js`), included the exact required reconnaissance statement, and answered Route C's objective accurately.
2. **The Exact Code Gate:** In `src/scout-combine.ts` line 144, status is derived by:
   `if (attempt.completionState === 'COMPLETE') return attempt.evaluation.reportStructurallyUsable ? 'READY' : 'LIMITED';`
   In `src/scout-interchangeability-runner.ts` lines 130-131, `reportStructurallyUsable` requires:
   `new RegExp('(?:^|\\n)#{1,3}\\s+' + section + '\\s*$', 'im')` for each of the 8 required sections.
3. **The Exact Syntax Mismatch:**
   - **Qwen** formatted all 8 section titles as plain uppercase lines with no Markdown hashes (e.g. `EXECUTIVE ANSWER`, `FACTS`, `INFERENCES`, etc.).
   - **Inkling** formatted sections with trailing colons and inline content with no Markdown hashes (e.g. `Executive answer: The top-level manifest...`, `FACTS:`, `INFERENCES:`, etc.).
   - Because neither model used Markdown heading hashes (`# `, `## `, or `### `), the regex failed for all 8 sections on both reports. This caused `reportStructurallyUsable` to evaluate to `false`, which forced `deriveCombineStatus` to assign `LIMITED`.
4. **Prompt vs Evaluator Asymmetry:** The prompt rendered to the models (`renderScoutPlay` in `src/scout-play.ts`) lists the result contract as simple bullet points (`- Executive answer`, `- FACTS`, etc.) and never instructs the models to format section names as Markdown headings.

==================================================
QUESTIONS TO ANSWER (FORENSIC AUDIT)
==================================================

### 1. Why exactly was Qwen classified LIMITED?

**Exact Code Path:**
1. `src/scout-combine.ts` line 144:
   ```typescript
   if (attempt.completionState === 'COMPLETE') return attempt.evaluation.reportStructurallyUsable ? 'READY' : 'LIMITED';
   ```
2. `src/scout-interchangeability-runner.ts` lines 130-131:
   ```typescript
   export const RESULT_SECTIONS = ['Executive answer', 'FACTS', 'INFERENCES', 'UNKNOWNS', 'CONTRADICTIONS', 'Relevant files / symbols', 'Recommended next step', 'Provenance'];
   export const REQUIRED_SCOUT_STATEMENT = 'This report is reconnaissance, not final architectural authority.';

   export function evaluateScoutReport(report: string, gameRoot: string): ScoutAttemptTelemetry['evaluation'] {
     const structurallyUsable = report.includes(REQUIRED_SCOUT_STATEMENT)
       && RESULT_SECTIONS.every((section) => new RegExp(`(?:^|\\n)#{1,3}\\s+${section.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\$&')}\\s*$`, 'im').test(report));
   ```

**Exact Persisted Evidence:**
In `openrouter-qwen-qwen3.8-27b-free-SCOUT-REPORT.md`:
- Line 5: `EXECUTIVE ANSWER` (no `#`)
- Line 8: `FACTS` (no `#`)
- Line 18: `INFERENCES` (no `#`)
- Line 22: `UNKNOWNS` (no `#`)
- Line 26: `CONTRADICTIONS` (no `#`)
- Line 29: `RELEVANT FILES / SYMBOLS` (no `#`)
- Line 34: `RECOMMENDED NEXT STEP` (no `#`)
- Line 37: `PROVENANCE` (no `#`)

Because Qwen omitted `# `, `## `, or `### ` before these titles, `re.test(report)` returned `false` for every single section. `structurallyUsable` evaluated to `false`. Therefore, `deriveCombineStatus` assigned `LIMITED`.

When tested in node during this forensic, adding `# ` to Qwen's 8 section headers immediately changed `reportStructurallyUsable` from `false` to `true`.

---

### 2. Why exactly was Inkling classified LIMITED?

**Exact Persisted Evidence:**
In `openrouter-thinkingmachines-inkling-free-SCOUT-REPORT.md`:
- Line 1: `Executive answer: The top-level manifest is package.json at the game root...` (no `#`, content on same line, trailing colon)
- Line 3: `FACTS:` (no `#`, trailing colon)
- Line 10: `INFERENCES:` (no `#`, trailing colon)
- Line 13: `UNKNOWNS:` (no `#`, trailing colon)
- Line 16: `CONTRADICTIONS:` (no `#`, trailing colon)
- Line 19: `Relevant files / symbols:` (no `#`, trailing colon)
- Line 22: `Recommended next step:` (no `#`, trailing colon)
- Line 25: `Provenance:` (no `#`, trailing colon)

Inkling failed the exact same regex condition:
1. It used no Markdown heading hashes (`# `, `## `, `### `).
2. It placed colons (`:`) immediately after the section names, which also violates the regex's end-of-line anchor `\\s*$`.
3. For `Executive answer`, the section content started on the same line as the header.

`re.test(report)` returned `false` for all 8 sections. `structurallyUsable` evaluated to `false`, and `deriveCombineStatus` assigned `LIMITED`.

---

### 3. Was the classification actually warranted?

**Analysis of Content vs Contract:**
Route C objective (`src/scout-combine.ts` line 84):
*"Identify this Game’s own top-level project manifest file (for example package.json, pyproject.toml, or an equivalent). State its declared project name and cite one real dependency, script, or configuration entry it defines. If no such manifest exists, truthfully report UNKNOWN and describe what evidence you found instead."*

- **Qwen's Report:**
  - Manifest: `package.json` at Game root.
  - Project name: `sideline-coach` (`displayName: "Sideline Coach"`).
  - Concrete entries: `ws` at `^8.21.3`, `test` and `compile` scripts, `coach.port` configuration.
  - Citations: Exact line numbers and Game-relative paths.
  - Epistemics: Clean separation of FACTS, INFERENCES, UNKNOWNS, and CONTRADICTIONS.
  - Required statement: Verbatim inclusion.
  - Filesystem verification: Evaluator successfully matched `out\extension.js` (`significantEvidenceMissing: null`).
- **Inkling's Report:**
  - Manifest: `package.json` at Game root.
  - Project name: `sideline-coach`.
  - Concrete entries: `ws` (`^8.21.3`), `compile` (`tsc -p ./`), `coach.port` (49152).
  - Epistemics: Clear, concise bullets.
  - Required statement: Verbatim inclusion.
  - Filesystem verification: Evaluator successfully matched `out\extension.js` (`significantEvidenceMissing: null`).

**Verdict:**
From a practical Scout perspective, the classification of `LIMITED` is **NOT warranted by content or capability**. Both models conducted excellent, bounded, read-only reconnaissance and provided truthful, verifiable answers.
The failure was purely syntactic: the evaluator enforces a rigid Markdown heading format (`#{1,3} <SectionName>`) that was **never specified in the prompt sent to the models**.

---

### 4. What does READY actually require today?

**The Exact Decision Tree in Code Today:**

```
Scout Tryout Execution
  │
  ├─► completionState === 'COMPLETE'
  │     │
  │     ├─► reportStructurallyUsable === true ──► READY
  │     │     │
  │     │     └─ Requires:
  │     │          1. report.includes("This report is reconnaissance, not final architectural authority.")
  │     │          2. Every one of 8 sections matches /(?:^|\n)#{1,3}\s+<SECTION>\s*$/im:
  │     │               - # Executive answer
  │     │               - # FACTS
  │     │               - # INFERENCES
  │     │               - # UNKNOWNS
  │     │               - # CONTRADICTIONS
  │     │               - # Relevant files / symbols
  │     │               - # Recommended next step
  │     │               - # Provenance
  │     │
  │     └─► reportStructurallyUsable === false ──► LIMITED
  │
  ├─► completionState === 'BLOCKED'
  │     ├─► error matches auth / sign-in / api key ──► AUTH ISSUE
  │     ├─► error matches rate-limit / quota / 429 ──► RATE LIMITED
  │     ├─► error matches model not found ──► UNAVAILABLE
  │     └─► other stream/provider error ──► PROVIDER UNSTABLE
  │
  ├─► completionState === 'FAILED' (process exit != 0) ──► CALL BACK LATER
  │
  └─► completionState === 'INTERRUPTED' or 'UNKNOWN' ──► UNKNOWN
```

**Notable Observation:**
`deriveCombineStatus` (`src/scout-combine.ts` line 144) does **not** check `significantEvidenceMissing` or `existingGameArtifactObserved`.
Even if a model cites existing files perfectly, missing a `#` header makes it `LIMITED`.
Conversely, Poolside Laguna S 2.1 was marked `READY` solely because it included `## ` headers before its sections.

---

### 5. Is one tryout enough evidence?

**Analysis of Scorecard Semantics:**
- In `src/scout-combine.ts` line 248, `CALLBACK_STATUSES` includes `'LIMITED'`.
- In `src/scout-combine.ts` lines 302-305:
  When a candidate is marked `LIMITED`, its `lastTryoutAt` is updated to `now`.
  Because it is not stale (staleness window is 7 days: `COMBINE_STALE_MS = 7 * 86_400_000`), `refreshConsideration` returns:
  `due: false` (*"The last Combine tryout is still inside the existing 7-day staleness window; do not retry immediately."*).
- Furthermore, in `src/scout-combine-formation-bridge.ts` lines 78-81:
  `if (card.currentStatus !== 'READY') return { eligible: false, reason: 'Combine scorecard status is LIMITED, not READY.' };`
  A candidate with status `LIMITED` is strictly excluded from Formation dispatch.

**Impact:**
Because of a single missing `#` character on its first tryout:
1. The prospect is barred from Formation snaps.
2. The prospect is frozen out of automated retry for 7 full days.
This turns what should be a minor formatting quirk into an effective 7-day suspension.

---

### 6. Careful Inspection of the Qwen Report

Qwen's report is an exceptionally high-quality reconnaissance artifact:
- It extracted the exact top-level manifest: `package.json`.
- It provided the Game-relative path: `package.json`.
- It cited exact line numbers for dependencies: `ws` on line 149.
- It identified scripts (`test`, `compile`) and configuration (`coach.port`).
- It separated inferences (VS Code extension) from facts (manifest contents).
- It noted unknowns (runtime behavior unverified, `node_modules` not inspected).
- It confirmed contradictions were absent.
- It provided provenance detailing the exact read-only globs and file reads performed.
- It included the required disclaimer verbatim.

**What Combine Believed Was Insufficient:**
Combine did not judge the content insufficient. Combine's automated evaluator executed a strict regex check:
`new RegExp('(?:^|\\n)#{1,3}\\s+FACTS\\s*$', 'im')`
Because Qwen output `FACTS` instead of `# FACTS`, the boolean flag `reportStructurallyUsable` became `false`. Everything else in Combine flowed deterministically from that single boolean.

==================================================
DESIRED OUTPUT
==================================================

### A. Qwen
**Exact Reason for LIMITED:**
Qwen formatted the 8 mandatory section names as plain uppercase text (`EXECUTIVE ANSWER`, `FACTS`, `INFERENCES`, `UNKNOWNS`, `CONTRADICTIONS`, `RELEVANT FILES / SYMBOLS`, `RECOMMENDED NEXT STEP`, `PROVENANCE`) instead of prefixing them with Markdown heading hashes (`# `, `## `, or `### `).
This caused `evaluateScoutReport` (`src/scout-interchangeability-runner.ts` line 131) to evaluate `reportStructurallyUsable` as `false`, which directly triggered `deriveCombineStatus` (`src/scout-combine.ts` line 144) to return `LIMITED`.

### B. Inkling
**Exact Reason for LIMITED:**
Inkling formatted the section names with trailing colons and placed inline text on the same line (e.g. `Executive answer: The top-level manifest...`, `FACTS:`, `INFERENCES:`, etc.) without Markdown heading hashes (`# `).
This failed the same regex (`(?:^|\\n)#{1,3}\\s+<SECTION>\\s*$`), resulting in `reportStructurallyUsable: false` and status `LIMITED`.

### C. READY Contract
Today, achieving `READY` requires:
1. `completionState === 'COMPLETE'` (process exit code 0, no unhandled stream error).
2. Verbatim presence of `This report is reconnaissance, not final architectural authority.`.
3. Every single one of the 8 required sections must appear on its own line starting with 1 to 3 `#` characters followed by whitespace, the exact section title (case-insensitive), and ending immediately at newline with no trailing colon or inline body text.

### D. Judgment
1. **Qwen (openrouter-qwen-qwen3.8-27b-free):**
   **Evaluator is overly strict.** Qwen produced an exemplary Scout report with rich evidence, line-number citations, and strict read-only compliance. Its failure was an unprompted syntax detail (prompt never requested Markdown heading hashes).
2. **Thinking Machines Inkling (openrouter-thinkingmachines-inkling-free):**
   **Evaluator is overly strict.** Inkling adhered to the read-only contract, cited valid artifacts, and separated facts from inferences. Placing colons after headers and putting content on the header line is a minor formatting difference, not an evidentiary or architectural failure.

### E. Smallest Repair (Conceptual — Reconnaissance Only)
If the coaching staff decides this strictness is counterproductive, there are two distinct, non-conflicting repair points:

1. **Prompt Contract Repair (`src/scout-play.ts` `renderScoutPlay`):**
   In `renderScoutPlay`, update the `RESULT CONTRACT` instructions to explicitly state:
   *"Format each of the following sections as a Markdown heading starting with # (# Section Name) on its own line:"*
   Currently, the prompt presents them as bullet points (`- Executive answer`), which inadvertently encourages models to output bulleted or unhashed text.
2. **Evaluator Regex Tolerance (`src/scout-interchangeability-runner.ts` `evaluateScoutReport`):**
   Relax the section regex to accept common structural delineations:
   - Allow optional Markdown hashes (`#{0,3}`)
   - Allow optional trailing colons (`:?`)
   - Example pattern: `(?:^|\\n)(?:#{1,3}\\s+|\\*{2})?${section}(?:\\*{2})?:?\\s*(?:$|\\n)`
   This preserves section detection while tolerating minor formatting variations that do not impair downstream synthesis.

### F. No-Repair Case
If the team maintains that strict Markdown headings (`# `) are an invariant required for machine parsing in downstream Formation synthesis:
- In future tryouts, Qwen and Inkling must output the 8 sections preceded by `# ` (or `## `), with no colons and no inline text on the heading line.
- However, because `LIMITED` candidates are given `due: false` for 7 days in `src/scout-combine.ts`, they will not receive another tryout during normal Combine runs until 2026-09-25 unless manually forced or requalified.

==================================================
ONE-SENTENCE CONCLUSION
==================================================
Qwen and Inkling were classified LIMITED solely because their section headers lacked Markdown `#` symbols—a strict evaluator regex requirement that was never stated in the Scout Play prompt—despite both models delivering fully accurate, verified read-only repository reconnaissance.

REPORT NAME: Qwen + Inkling LIMITED Scorecard Forensic V0.1
REPORT TIMESTAMP: 2026-09-18 18:26 MDT
